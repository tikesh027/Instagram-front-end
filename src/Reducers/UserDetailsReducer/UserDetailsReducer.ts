import { TACTION } from "../../Store/storeTypes";

const initalState = {
    data: null,
    
}

function userDetails(state, action: TACTION){
    switch(action.type){
        default: {
            return state;
        }
    }
}

export default userDetails;